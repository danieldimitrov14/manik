function reverse(sentence){
    let string = "";
    for(let i = sentence.length - 1; i >= 0; i--){
        string += sentence[i]
        
    }
    console.log(string)
}
reverse('It ’ s lights out and away we go')
